## Agent skills

### Issue tracker

Issues and specs for this repo live in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the default Matt Pocock engineering-skill triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

Use a single-context domain-doc layout for this repo. See `docs/agents/domain.md`.

### First-release implementation

When implementing or planning the first Android release, start with `README.md` for the authoritative spec, ticket entry points, and resource context. Read the chosen issue and its blocking dependencies before implementation.
