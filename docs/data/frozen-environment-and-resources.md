# Frozen 简中环境与卡图资源证据（T01 / #2）

本文件是本票的可读产物索引。机器可读产物：

- `data/environment/zh-cn-standard-2025-06-05.json`：冻结环境（赛制、构筑限制、勘误、商品日期、来源与本地证据摘要）。
- `data/evidence/asar-2025060501-sample.json`：Z 盘 asar 资源的有界抽样证据（编号、哈希、语言判定、编号映射）。

## 1. 冻结环境

| 项 | 值 | 来源 |
| --- | --- | --- |
| 快照日期 | 2025-06-05 | 本票截止日 |
| 赛制 | 简中「标准赛制」 | <https://www.pokemon.cn/tcg-rules-regulation> |
| 合法性依据 | 卡面左下「赛制标记」，当前为 **E / F / G**，另加 8 种基本能量卡 | 官方 2024-12-22 公告 <https://www.pokemon.cn/tcg/other/17144.html>（2025-01-17 起生效，D 退出） |
| 构筑 | 恰好 60 张；同名 ≤4；基本能量不限张数；至少 1 张基础宝可梦；同名【棱镜之星】≤1 | 见 JSON 内 `construction` 字段的官方原文与链接 |
| 勘误模型 | 无统一勘误文档，逐条公告于 `/tcg/other/`；截止日前最近一条 2025-05-20 | <https://www.pokemon.cn/tcg/other/17127.html> |

**明确的产品编号与赛制标记区别**：商品编号（如 `CSV1C`、`CS5aC`）**不是**赛制标记，官方公告原文已说明。

### 未解决（不得据猜测填补）

1. **ACE SPEC（王牌）与「光辉」(Radiant) 的官方简中一张上限条文未找到**。国际环境有该限制，但官方简中页面检索不到；在拿到来源前不得写成规则。
2. **逐弹卡牌商品编号（CS*/CBB*）→ 卡牌编号的官方对照表未找到**。
3. **官方没有网页版卡牌数据库**；官方卡表查询在微信小程序「宝可梦卡牌会员」，无 HTTP 接口（<https://www.pokemon.cn/tcg/other/17199.html>）。

## 2. Z 盘 asar 资源（有界、只读抽样结论）

资源：`中文卡牌图片资源包_2025060501.asar`（Z 盘候选资源，只读）。
大小 5 615 709 737 字节；SHA-256 `BE63E6D3…4AA92353`；`meta.json` 记录 `provider: Kuyo`、`version: 2025060501`、`hashType: SHA-256`、8370 条 `id+hash`。

**结论（均已用工具复现，未抽样项的结论不成立）**：

1. **可导出可读图像**。索引 8371 项（8370 卡图 + `meta.json`），每项是一个 `UnityFS` 资源包，内含 1024×1024 主卡面与 512×512 副纹理；用 `tools/card-image-export/export_textures.py`（UnityPy）可导出 PNG。
2. **`_en_` / 资产路径里的 `EN` 不是画面语言证据**。抽样 21 项：`zh-Hant` 17、`zh-Hans` 3、`en` 1。SWSH/SV 世代样本以**繁体中文**为主，间有简中与英文卡面；BW/SM 世代样本中出现简中卡面。
3. **编号映射是「id 编号 ≠ 卡面印刷编号」**。id 采用英文套卡代号与英文卡号（如 `files/sv1_en_170`），而卡面印刷的是中文商品的编号（如 `SV1F 069/078`）。`MaterialManifest._p`（如 `Assets/Cards/FullCard/SV1/EN/001/…mat`）只是资产路径，同样不能当作语言或简中编号依据。
4. **包内语言不一致**，且部分「简中文字」卡面仍套用英文模板（BW10 `嘉德丽雅` 卡面顶部为英文 `TRAINER/SUPPORTER`，印刷 `78/101`），提示其可能来自客户端渲染而非官方简中印刷。**因此不能把该包当作简中卡牌文字或简中印刷编号的权威来源。**
5. **来源与可分发范围未解决**：包内缺少版权/许可声明。原始包必须作为本机、用户配置的输入，不提交仓库、不外传。

抽样与工具链：

- `tools/asar-inspect/cli.mjs`（只读 asar 索引与取件）、`tools/card-image-export/export_textures.py`、`tools/card-image-export/screen_evidence.mjs`（有界流水线）、`tools/ocr/ocr-windows.ps1`（系统 OCR）。
- 抽样为显式枚举（每次 `--entry`），未逐项遍历整个包；NAS 只访问 README 已命名的该文件。

## 3. 四套卡表：未交付（阻塞，非降级）

四套「精确到印刷编号 + 完整简中文字」的 60 张卡表**没有产出**，因为本会话内找不到可合法取得、可核实的简中卡牌级数据源：

- 官方：只有微信小程序卡表，无 HTTP 接口；官网无卡库，商品文章只展示少量卡图（`figure.t-image` 提供卡名 + 图片，可作为**候选证据**，但每张的印刷编号与效果文字仍需逐图人工读取）。
- 社区库 `tcg.mik.moe`：确有简中卡牌级数据，但其 `/ai-access.json` 要求**每次请求前由用户逐次确认并逐次提供意图句**；本会话无法满足，故**未采用**（不替代、不绕行）。
- 用户 Z 盘资源包：如上，语言混杂、印刷编号非简中编号，不能作为文字来源。

按母规格「无法核实的条目替换或排除，不能留猜测项」，此处不填占位卡表。

### 下游接入所需的最小输入（二选一）

1. **官方渠道**：由用户在微信小程序「宝可梦卡牌会员」按所选商品导出/截图卡表，或提供官方 `CS*` 商品编号 → 卡号对照；随后本票可用官方文章图片逐卡复核并补齐四套卡表。
2. **社区渠道**：用户明确授权对 `tcg.mik.moe` 的逐次请求意图与确认（提供意图句并授权），届时可定点拉取 ≤4 套卡组所需卡牌（约 40–60 张）并逐张对照官方图片。

拿到任一输入后，剩余工作为：定卡组 → 生成 `data/decks/{A,B,C,D}.json` → 生成去重效果清单（含完整简中文字、规则身份、所需选择、重印关系）→ 效果覆盖矩阵 → 合法性校验。

## 4. 已核实的其他事实（供下游参考）

- 官方商品文章（`/tcg/product/<id>.html`）以 `figure.t-image` 结构给出「卡名(h4) + 卡图」，可作候选卡证据来源；示例已取证的页面：16336（交相辉映，2023-01-13，31 张卡图）、2024010501（2024-01-05 礼盒，5 张卡图）。
- 本地证据摘要（SHA-256）记录在环境 JSON 的 `local_evidence_digests` 字段。
